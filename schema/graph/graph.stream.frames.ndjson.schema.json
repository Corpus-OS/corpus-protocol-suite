{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://adaptersdk.org/schemas/graph/graph.stream.frames.ndjson.schema.json",
  "title": "Graph NDJSON Stream Frames (Union)",
  "description": "Each NDJSON line MUST be one of the canonical Graph streaming frames (data/end/error). Wire-stable across v1.x.",
  "oneOf": [
    { "$ref": "https://adaptersdk.org/schemas/graph/graph.stream.frame.data.json" },
    { "$ref": "https://adaptersdk.org/schemas/graph/graph.stream.frame.end.json" },
    { "$ref": "https://adaptersdk.org/schemas/graph/graph.stream.frame.error.json" }
  ],
  "$comment": "Transport rules (enforced by stream validator, not JSON Schema): exactly one terminal frame (end OR error), and no data after terminal. Frame size SHOULD be ≤ 1 MiB; servers MAY emit heartbeats as empty/metadata-only data frames."
}
