{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://corpusos.com/schemas/embedding/embedding.envelope.request.json",
  "title": "embedding.envelope.request",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "op",
    "ctx",
    "args"
  ],
  "properties": {
    "op": {
      "type": "string",
      "enum": [
        "embedding.capabilities",
        "embedding.count_tokens",
        "embedding.embed",
        "embedding.embed_batch",
        "embedding.health"
      ]
    },
    "ctx": {
      "$ref": "https://corpusos.com/schemas/common/operation_context.json"
    },
    "args": {
      "type": "object"
    },
    "protocol": {
      "type": "string",
      "const": "embedding/v1.0"
    },
    "component": {
      "type": "string",
      "const": "embedding"
    }
  }
}
