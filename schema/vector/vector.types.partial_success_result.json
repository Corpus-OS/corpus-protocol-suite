{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://corpusos.com/schemas/vector/vector.types.partial_success_result.json",
  "title": "Vector Partial-Success Result",
  "description": "Result payload for transport-level partial success (code=PARTIAL_SUCCESS) per §23.4. Captures per-item outcomes for batch operations with stable, machine-actionable fields.",
  "type": "object",
  "additionalProperties": false,
  "required": ["successes", "failures", "items"],
  "properties": {
    "successes": {
      "type": "integer",
      "minimum": 1,
      "description": "Number of successful items (MUST be >= 1 for PARTIAL_SUCCESS)."
    },
    "failures": {
      "type": "integer",
      "minimum": 1,
      "description": "Number of failed items (MUST be >= 1 for PARTIAL_SUCCESS)."
    },
    "items": {
      "type": "array",
      "minItems": 2,
      "maxItems": 10000,
      "description": "Per-item outcomes in the same order as input.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["index", "ok"],
        "properties": {
          "index": {
            "type": "integer",
            "minimum": 0,
            "description": "Zero-based index into the original request batch."
          },
          "ok": {
            "type": "boolean",
            "description": "True if this item succeeded; false if it failed."
          },
          "result": {
            "description": "Present when ok=true: the result for this item.",
            "$ref": "https://corpusos.com/schemas/vector/vector.types.vector_match.json"
          },
          "code": {
            "type": "string",
            "pattern": "^[A-Z0-9_]{1,64}$",
            "description": "Present when ok=false: normalized error code."
          },
          "message": {
            "type": "string",
            "minLength": 1,
            "maxLength": 2000,
            "description": "Present when ok=false: human-readable, SIEM-safe summary."
          },
          "details": {
            "type": "object",
            "description": "Optional, SIEM-safe structured context for failures.",
            "additionalProperties": true
          }
        },
        "allOf": [
          {
            "if": {
              "properties": { "ok": { "const": true } }
            },
            "then": {
              "required": ["result"]
            }
          },
          {
            "if": {
              "properties": { "ok": { "const": false } }
            },
            "then": {
              "required": ["code", "message"]
            }
          }
        ]
      }
    }
  }
}
