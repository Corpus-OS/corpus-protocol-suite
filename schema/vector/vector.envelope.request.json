{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://corpusos.com/schemas/vector/vector.envelope.request.json",
  "title": "Vector Request Envelope",
  "description": "Wire-first canonical request envelope for vector component operations.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "op": {
      "type": "string",
      "enum": [
        "vector.query",
        "vector.upsert",
        "vector.delete",
        "vector.create_namespace",
        "vector.delete_namespace",
        "vector.capabilities",
        "vector.health"
      ]
    },
    "ctx": {
      "$ref": "https://corpusos.com/schemas/common/operation_context.json"
    },
    "args": {
      "type": "object",
      "description": "Operation-specific arguments payload. Shape is determined by the vector operation type.",
      "additionalProperties": true
    }
  },
  "required": ["op", "ctx", "args"],
  "$comment": "Follows common envelope.request shape per §2.4; args validation is delegated to per-operation schemas."
}
