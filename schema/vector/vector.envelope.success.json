{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://corpusos.com/schemas/vector/vector.envelope.success.json",
  "title": "Vector Success Envelope",
  "description": "Canonical success envelope for vector operations per protocol §2.4.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "ok": { "type": "boolean", "const": true },
    "code": { "type": "string", "const": "OK" },
    "ms": { "type": "number", "minimum": 0 },
    "result": {
      "type": "object"
    }
  },
  "required": ["ok", "code", "ms", "result"]
}