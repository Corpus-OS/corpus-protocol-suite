{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://adaptersdk.org/schemas/vector/vector.types.vector.json",
  "title": "Vector",
  "description": "Canonical Vector representation used by Vector Protocol operations.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1,
      "maxLength": 256,
      "pattern": "^[A-Za-z0-9._~:-]{1,256}$",
      "description": "Stable VectorID."
    },
    "vector": {
      "type": "array",
      "minItems": 1,
      "maxItems": 8192,
      "items": {
        "type": "number"
      },
      "description": "Dense float vector; length MUST equal the namespace dimension. NaN/±Inf MUST be rejected by implementations."
    },
    "metadata": {
      "type": ["object", "null"],
      "description": "JSON-serializable metadata (low cardinality). MUST be SIEM-safe.",
      "additionalProperties": true
    },
    "namespace": {
      "type": ["string", "null"],
      "maxLength": 256,
      "pattern": "^[A-Za-z0-9._:-]{1,256}$",
      "description": "Optional per-item namespace; if provided, MUST equal request-level namespace."
    }
  },
  "required": ["id", "vector"],
  "$comment": "Dimension conformity is enforced against capabilities.limits.dimension at runtime."
}
