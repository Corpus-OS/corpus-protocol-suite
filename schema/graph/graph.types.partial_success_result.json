{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://corpusos.com/schemas/graph/graph.types.partial_success_result.json",
  "title": "Graph Partial Success Result",
  "description": "Canonical transport-level partial-success payload for non-atomic Graph batch operations. Used with success envelopes that set code=\"PARTIAL_SUCCESS\".",
  "type": "object",
  "additionalProperties": false,
  "required": ["processed_count", "failed_count", "failures", "successes", "items"],
  "properties": {
    "processed_count": {
      "type": "integer",
      "minimum": 0,
      "description": "Total number of items processed (per §3.4 BatchResult pattern)."
    },
    "failed_count": {
      "type": "integer",
      "minimum": 0,
      "description": "Number of items that failed (per §3.4 BatchResult pattern)."
    },
    "successes": {
      "type": "integer",
      "minimum": 0,
      "description": "Number of successful items."
    },
    "failures": {
      "type": "array",
      "description": "Array of failure details (per §3.4 BatchResult pattern).",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "error": {
            "type": "string",
            "description": "Error code for the failed item."
          },
          "detail": {
            "type": "string",
            "description": "Human-readable detail about the failure."
          },
          "index": {
            "type": "integer",
            "minimum": 0,
            "description": "Optional zero-based index of the failed item."
          },
          "id": {
            "type": "string",
            "description": "Optional ID of the failed item."
          }
        },
        "required": ["error", "detail"]
      }
    },
    "items": {
      "type": "array",
      "minItems": 1,
      "maxItems": 10000,
      "description": "Per-item result entries in the same order as input.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["index", "ok"],
        "properties": {
          "index": {
            "type": "integer",
            "minimum": 0,
            "description": "Zero-based index of the input item."
          },
          "ok": {
            "type": "boolean",
            "description": "True if the item succeeded, false if it failed."
          },
          "result": {
            "description": "Item-specific success payload (e.g., created IDs, rows). Shape depends on the batch op.",
            "oneOf": [
              {
                "$ref": "https://corpusos.com/schemas/graph/graph.types.batch_op.json"
              },
              {
                "$ref": "https://corpusos.com/schemas/graph/graph.types.row.json"
              },
              {
                "$ref": "https://corpusos.com/schemas/graph/graph.id.success.json"
              },
              {
                "$ref": "https://corpusos.com/schemas/graph/graph.ack.success.json"
              },
              {
                "type": "object",
                "additionalProperties": true,
                "not": {
                  "anyOf": [
                    {"required": ["id"]},
                    {"required": ["status"]},
                    {"required": ["rows"]}
                  ]
                }
              }
            ]
          },
          "code": {
            "type": "string",
            "pattern": "^[A-Z0-9_]{1,64}$",
            "description": "Normalized error code for failed items (e.g., BAD_REQUEST, NOT_SUPPORTED)."
          },
          "message": {
            "type": "string",
            "minLength": 1,
            "maxLength": 2000,
            "description": "Human-readable, SIEM-safe message for failed items."
          },
          "details": {
            "type": "object",
            "additionalProperties": true,
            "description": "Low-cardinality, SIEM-safe context for failed items (no PII/prompts/queries)."
          }
        },
        "allOf": [
          {
            "if": {
              "properties": { "ok": { "const": true } },
              "required": ["ok"]
            },
            "then": {
              "required": ["result"],
              "properties": {
                "code": { "not": {} },
                "message": { "not": {} }
              }
            }
          },
          {
            "if": {
              "properties": { "ok": { "const": false } },
              "required": ["ok"]
            },
            "then": {
              "required": ["code", "message"],
              "properties": {
                "result": { "not": {} }
              }
            }
          }
        ],
        "patternProperties": {
          "^(vendor:)[a-z0-9_.:-]+$": {
            "type": ["string", "number", "boolean", "object", "array", "null"],
            "description": "Namespaced vendor extension fields."
          }
        }
      }
    }
  },
  "$comment": "Invariants (enforced in tests/logic, not JSON Schema): (1) processed_count == items.length; (2) failed_count == failures.length; (3) successes + failed_count == processed_count; (4) Items preserve input order; (5) No silent drops of failed items."
}
