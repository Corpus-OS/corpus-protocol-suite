{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://adaptersdk.org/schemas/graph/graph.stream.frame.data.json",
  "title": "Graph Stream Frame (data)",
  "type": "object",
  "additionalProperties": false,
  "required": ["event", "data"],
  "properties": {
    "event": { "type": "string", "const": "data" },
    "data": { "$ref": "https://adaptersdk.org/schemas/graph/graph.types.row.json" }
  },
  "$comment": "Frame size SHOULD be ≤ 1 MiB; heartbeats MAY be empty-row frames per spec."
}
