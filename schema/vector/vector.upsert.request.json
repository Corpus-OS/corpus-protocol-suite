{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://corpusos.com/schemas/vector/vector.upsert.request.json",
  "title": "vector.upsert args",
  "description": "Arguments for op: vector.upsert per protocol §15.3.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "vectors": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "https://corpusos.com/schemas/vector/vector.types.vector.json" }
    },
    "namespace": {
      "type": "string",
      "default": "default",
      "maxLength": 256,
      "pattern": "^[A-Za-z0-9._:-]{1,256}$"
    }
  },
  "required": ["vectors"]
}