{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://adaptersdk.org/schemas/embedding/embedding.envelope.error.json",
  "title": "Embedding Error Envelope",
  "type": "object",
  "allOf": [
    { "$ref": "https://adaptersdk.org/schemas/common/envelope.error.json" },
    {
      "type": "object",
      "required": ["protocol", "component"],
      "properties": {
        "protocol": { "const": "embedding/v1.0" },
        "component": { "const": "embedding" }
      }
    }
  ]
}
